<form method="post" action="{{ path('app_argument_delete', {'id': argument.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ argument.id) }}">
    <button class="btn">Delete</button>
</form>
